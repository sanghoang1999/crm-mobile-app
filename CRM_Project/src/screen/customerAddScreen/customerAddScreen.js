import React, {useEffect} from 'react';
import {View} from 'react-native';

import AppStatusBar from '../../component/AppStatusBar/AppStatusBar';

const CustomerAddScreen = ({navigation, route}) => {
  return (
    <View>
      <AppStatusBar />
    </View>
  );
};
export default CustomerAddScreen;
