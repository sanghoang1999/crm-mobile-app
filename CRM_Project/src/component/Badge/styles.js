import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  badge: {
    width: 30,
    height: 30,
    position: 'absolute',
    bottom: 4,
    right: 4,
    backgroundColor: '#CCCCCC',
    alignItems: 'center',
    borderRadius: 50,
  },
});
